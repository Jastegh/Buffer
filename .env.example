# Buffer runs fully without any of these values.
# All explanations on the dashboard are generated deterministically in lib/insights.ts.
#
# Setting a key only enables the optional /api/copilot endpoint, which rephrases
# already-calculated aggregates. Raw transaction data is never sent.

# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
